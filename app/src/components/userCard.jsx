/*  
 const UserCard =({user})=>{
    return (
            <div key={user.id} className="usersDiv">
            <div><button>Offline</button></div>
            <div>{user.fullname}</div>
            <div>{user.username}</div>
            <div>{user.email}</div>
            <div>{user.phone}</div>
            <div><img src={info} alt="info"></img></div>
          </div>)
           }
export default UserCard; */
